malloc : return vopid pointer . Hence it needs to be type casted 
         It takes one single argument that is the size.
         It initialize the memory block with garbage value.

calloc : calloc is similar to malloc the only difference is that it initilizes the meoery block with zero.
         It Takes two arguments one is size and the pointer.


realloc : realloc extra memory allocation is done with same two arguments.



function_pointer : return_type(*pointer)(parameters)
